#include<stdio.h> 

int main()
{
    int a;
    printf("Enter The Size of Array : ");
    scanf("%d",&a);
    int n[a];
    for(int i=0;i<a;i++)
    {
        printf("Enter The [%d] Element : ",i+1);
        scanf("%d",&n[i]);
    }
    printf("Number of negative numbers in Array elements = ");
    for(int i=0;i<a;i++)
    {
        if(n[i] < 0)
        {
            printf("%d \t",n[i]);
        }
    }
     return 0;
}
